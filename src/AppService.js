export const onHandeEnter = async (event) => {
  return await new Promise((resolve) => {
    setTimeout(() => {
      resolve([{}, {}, {}]);
    }, 3000);
  });
};

export const markTaskCompleted = async (id) => {
  return await new Promise((resolve) => {
    setTimeout(() => {
      resolve([{}, {}, {}]);
    }, 3000);
  });
};

export const markTaskUncompleted = async (id) => {
  return await new Promise((resolve) => {
    setTimeout(() => {
      resolve([{}, {}, {}]);
    }, 3000);
  });
};

export const onHandleFavorite = async (id) => {
  return await new Promise((resolve) => {
    setTimeout(() => {
      resolve([{}, {}, {}]);
    }, 3000);
  });
};
